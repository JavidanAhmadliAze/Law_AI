# Minimal deps the DAG tasks need beyond the Airflow image.
# Keep in sync with pyproject.toml versions.
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
pymupdf>=1.24
opensearch-py[async]>=2.6
boto3>=1.34
structlog>=24.1
psycopg2-binary>=2.9
